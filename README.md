# Persephone Cliente Pyhton #

Biblioteca desenvolvida para tratar as solicitações e prepará-las para serem enviadas para o projeto Persephone.

```
pip install poetry
python -m poetry build
python -m poetry publish
```